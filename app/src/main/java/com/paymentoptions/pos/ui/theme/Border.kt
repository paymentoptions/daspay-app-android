package com.paymentoptions.pos.ui.theme

import androidx.compose.foundation.BorderStroke
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

val noBorder = BorderStroke(0.dp, Color.Transparent)

val borderThinPrimary100 = BorderStroke(1.dp, primary100.copy(alpha = 0.2f))
val borderThickPrimary100 = BorderStroke(2.dp, primary100.copy(alpha = 0.2f))

val borderThinPrimary500 = BorderStroke(1.dp, primary500.copy(alpha = 0.2f))
val borderThickPrimary500 = BorderStroke(2.dp, primary500.copy(alpha = 0.2f))